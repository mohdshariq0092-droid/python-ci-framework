name: Reusable Python CI Pipeline

on:
  workflow_call:
    inputs:
      project_type:
        required: true
        type: string
      python_version:
        required: false
        default: "3.11"
        type: string
      deploy:
        required: false
        default: false
        type: boolean
      main_module:
        required: true
        type: string
      test_path:
        required: false
        default: tests
        type: string

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout project
        uses: actions/checkout@v4

      # Checkout the CI framework repo (where scripts live)
      - name: Checkout framework
        uses: actions/checkout@v4
        with:
          repository: mohdshariq0092-droid/python-ci-framework
          ref: v1   # framework version
          path: ci-framework  # checkout under this folder

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python_version }}

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest wheel setuptools
#          pip install -e .

      - name: Build wheel
        run: python -m build --wheel --outdir dist

      - name: Install wheel
        run: pip install dist/*.whl

      - name: Build
        run: bash ci-framework/scripts/build.sh ${{ inputs.project_type }}

      - name: Test
        run: bash ci-framework/scripts/test.sh

      # Run unit tests + coverage
      - name: Run unit tests + coverage
        working-directory: ${{ github.workspace }}
        run: pytest --cov=${{ inputs.main_module }} tests/

      # Run linting
      - name: Run linting
        working-directory: ${{ github.workspace }}
        run: flake8 -v .

      # Run pylint
      - name: Run pylint
        working-directory: ${{ github.workspace }}
        run: pylint ${{ inputs.main_module }} --fail-under=8.0

      # Run security scan (Bandit)
      - name: Security scan
        working-directory: ${{ github.workspace }}
        run: bandit -r ${{ inputs.main_module }}

      - name: Package
        run: bash ci-framework/scripts/package.sh

      - name: Deploy
        if: ${{ inputs.deploy }}
        run: bash ci-framework/scripts/deploy.sh
